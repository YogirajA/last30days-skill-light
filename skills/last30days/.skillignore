# Hermes scans from this skill directory, not the repository root.
# This is a slimmed "light" fork: the non-runtime dev/eval/test artifacts and
# demo assets that were previously listed here have been removed from the tree,
# so there is nothing left to exclude from install-time scans.
